name: example
description: A pro_validator example app.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  pro_validator:
    path: ../

dev_dependencies:
  pro_lints: ^3.0.1

flutter:
  uses-material-design: true
